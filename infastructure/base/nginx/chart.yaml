apiVersion: source.toolkit.fluxcd.io/v1
kind: HelmChart
metadata:
  name: nginx
  namespace: nginx
spec:
  chart: nginx-ingress
  version: "1.2.x"
  interval: 5m
  sourceRef:
    kind: HelmRepository
    name: nginx

